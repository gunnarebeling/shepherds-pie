<nav class="navbar navbar-expand-lg navbar-dark bg-dark p-4">
  <div class="container-fluid">
    <!-- Brand Link -->
    <%= link_to 'Brand', root_path, class: 'navbar-brand' %>
    
    <!-- Navbar Toggle Button for Mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <!-- Home Link -->
        <li class="nav-item">
          <%= link_to 'Home', root_path, class: 'nav-link text-light' %>
        </li>

        <!-- Conditional Links for Signed-In Employee -->
        <% if employee_signed_in? %>
          <li class="nav-item">
            <%= button_to 'Sign Out', destroy_employee_session_path, method: :delete, class: 'btn btn-link nav-link text-light' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Update Profile', edit_employee_registration_path, class: 'nav-link text-light' %>
          </li>
          <li class="nav-item">
            <span class="nav-link text-light">Welcome, <%= current_employee.name %></span>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign In', new_employee_session_path, class: 'nav-link text-light' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>